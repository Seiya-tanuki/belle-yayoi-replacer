interface:
  display_name: "Yayoi replacer"
  short_description: "Run line-aware deterministic replacement for Yayoi import CSV using canonical specs + lexicon + client_cache + defaults"
policy:
  allow_implicit_invocation: false

